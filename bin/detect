#!/bin/sh

if [ $TELEGRAF_CONF ]; then TELEGRAF_CONF=$1/$TELEGRAF_CONF; fi
TELEGRAF_CONF=${TELEGRAF_CONF-$1/telegraf.conf}

if [ -f $TELEGRAF_CONF ]; then
    echo "Found telegraf.conf ($TELEGRAF_CONF)"
    exit 0
else
    exit 1
fi

